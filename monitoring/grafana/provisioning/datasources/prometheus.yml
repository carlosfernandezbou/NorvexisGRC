apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Nombre del servicio de Prometheus en la red de docker-compose
    url: http://prometheus:9090
    isDefault: true
    editable: true
